# searches the target string for a given substring starting at the end and returns the highest index in s where substring <sub> is found (will return -1 if not found):

s = "foo bar foo baz foo qux".rfind('foo')

print(s)  # -> 16